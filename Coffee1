-- [[ GREEDY GROWERS - ULTIMATE GUI FIX ]] --
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local LP = Players.LocalPlayer

-- ປ້ອງກັນການເປີດຊ້ຳ
if CoreGui:FindFirstChild("UltimateAutoBuyHub") then
    CoreGui.UltimateAutoBuyHub:Destroy()
end

local SG = Instance.new("ScreenGui")
SG.Name = "UltimateAutoBuyHub"
SG.Parent = CoreGui
SG.ResetOnSpawn = false

local Frame = Instance.new("Frame", SG)
Frame.Size = UDim2.new(0, 260, 0, 290)
Frame.Position = UDim2.new(0.5, -130, 0.3, -145)
Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
Frame.Active = true
Frame.Draggable = true
Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 12)

local UIStroke = Instance.new("UIStroke", Frame)
UIStroke.Color = Color3.fromRGB(0, 170, 255)
UIStroke.Thickness = 2

local Title = Instance.new("TextLabel", Frame)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.Text = "🌱 Auto Buy Seed Hub"
Title.TextColor3 = Color3.fromRGB(0, 170, 255)
Title.TextSize, Title.Font = 15, Enum.Font.SourceSansBold
Title.BackgroundTransparency = 1

-- ສະຖານະການເລືອກລະດັບ
local selectedRarities = {
    ["Normal"] = false,
    ["Rare"] = false,
    ["Epic"] = false,
    ["Legendary"] = false
}

-- ຟັງຊັນສ້າງປຸ່ມເລືອກ (Checkbox)
local function createCheckbox(name, posY, rarityKey)
    local btn = Instance.new("TextButton", Frame)
    btn.Size = UDim2.new(0.9, 0, 0, 35)
    btn.Position = UDim2.new(0.05, 0, 0, posY)
    btn.Text = "❌ " .. name .. " [OFF]"
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize, btn.Font = 13, Enum.Font.SourceSansBold
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    
    btn.MouseButton1Click:Connect(function()
        selectedRarities[rarityKey] = not selectedRarities[rarityKey]
        if selectedRarities[rarityKey] then
            btn.Text = "✅ " .. name .. " [ON]"
            btn.BackgroundColor3 = Color3.fromRGB(0, 150, 80)
        else
            btn.Text = "❌ " .. name .. " [OFF]"
            btn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        end
    end)
    return btn
end

createCheckbox("Normal (ທຳມະດາ)", 45, "Normal")
createCheckbox("Rare (ຫາຍາກ)", 85, "Rare")
createCheckbox("Epic", 125, "Epic")
createCheckbox("Legendary", 165, "Legendary")

-- ປຸ່ມເປີດ/ປິດ ລະບົບຫຼັກ (Master Toggle)
local ToggleBtn = Instance.new("TextButton", Frame)
ToggleBtn.Size = UDim2.new(0.9, 0, 0, 40)
ToggleBtn.Position = UDim2.new(0.05, 0, 0, 215)
ToggleBtn.Text = "Master Status: OFF"
ToggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextSize, ToggleBtn.Font = 14, Enum.Font.SourceSansBold
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 8)

local autoActive = false
ToggleBtn.MouseButton1Click:Connect(function()
    autoActive = not autoActive
    if autoActive then
        ToggleBtn.Text = "Master Status: ON"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 80)
    else
        ToggleBtn.Text = "Master Status: OFF"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    end
end)

-- ລະບົບຊື້ອັດຕະໂນມັດໂດຍຢືນຢູ່ບ່ອນເກົ່າ
task.spawn(function()
    while task.wait(0.2) do
        if autoActive then
            pcall(function()
                for _, v in ipairs(workspace:GetDescendants()) do
                    if v:IsA("ProximityPrompt") then
                        local parentPart = v.Parent
                        if parentPart and parentPart:IsA("BasePart") then
                            local shouldBuy = false
                            
                            for rarityKey, isSelected in pairs(selectedRarities) do
                                if isSelected then
                                    for _, desc in ipairs(parentPart:GetDescendants()) do
                                        if desc:IsA("TextLabel") and desc.Text:lower():find(rarityKey:lower()) then
                                            shouldBuy = true
                                            break
                                        end
                                    end
                                end
                                if shouldBuy then break end
                            end
                            
                            if shouldBuy then
                                v.MaxActivationDistance = 999999
                                v.RequiresLineOfSight = false
                                fireproximityprompt(v)
                            end
                        end
                    end
                end
            end)
        end
    end
end)
